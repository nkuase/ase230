# Q2. Connect PHP Server with MySQL

---

## Question 2.1

CRUD operations require coordination between the web application, the database abstraction layer, and the database server. Match each component to its role:

| Component | Responsibility |
|---|---|
| PHP Server | ??1 |
| PDO | ??2 |
| MySQL Server | ??3 |

**Answer:**

---

## Question 2.2

Write the SQL commands in the MySQL shell to:
1. Create a database named `studentdb` only if it does not already exist.
2. Select `studentdb` as the active database for subsequent queries.

**Answer:**

---

## Question 2.3

Write the SQL `CREATE TABLE` statement to create the `students` table with the following schema:
- `id`: integer, auto-incrementing primary key
- `name`: variable-length text up to 100 characters
- `email`: variable-length text up to 150 characters
- `age`: integer
- `major`: variable-length text up to 100 characters
- `year`: integer

**Answer:**

---

## Question 2.4

After creating the database and table, what MySQL shell commands verify that:
1. `studentdb` was successfully created among available databases?
2. `students` exists as a table inside `studentdb`?
3. The column names and types match the expected schema definition?

**Answer:**

---

## Question 2.5

Why should the PHP application connect using a dedicated user account (such as `ase230`) rather than the MySQL `root` administrative account?

**Answer:**

---

## Question 2.6

Write the MySQL commands to create the `ase230` user with password `ase230pass` and grant full CRUD permissions on all tables in `studentdb`.

**Answer:**

---

## Question 2.7

In the MySQL account definition `'ase230'@'localhost'`, what does `@'localhost'` signify? Can this account be used to connect to MySQL from a remote machine across the Internet?

**Answer:**

---

## Question 2.8

Explain what each part of this PDO DSN (Data Source Name) string specifies:

```php
$dsn = "mysql:host=localhost;dbname=studentdb;charset=utf8mb4";
```

**Answer:**

---

## Question 2.9

Complete the following PHP code to instantiate a PDO connection that throws exceptions on database errors and returns rows as associative arrays:

```php
$pdo = new PDO($dsn, $username, $password, [
    PDO::ATTR_ERRMODE => PDO::??1,
    PDO::ATTR_DEFAULT_FETCH_MODE => PDO::??2,
]);
```

**Answer:**

---

## Question 2.10

What command starts PHP's built-in development server from the project directory, what URL verifies the connection script in your browser, and what message confirms success?

**Answer:**

---

## Question 2.11

What is the fundamental two-step PDO pattern that repeats across Create, Read, Update, and Delete operations?

**Answer:**

---

## Question 2.12

Complete the PHP code from `index_pdo.php` to insert a new student using named parameter placeholders:

```php
$sql = "INSERT INTO students (name, email, age, major, year)
        VALUES (:name, :email, :age, :major, :year)";

$stmt = $pdo->??1($sql);
$stmt->??2([
    ':name' => 'Student 1',
    ':email' => 'student1@example.com',
    ':age' => 20,
    ':major' => 'Computer Science',
    ':year' => 2,
]);
```

**Answer:**

---

## Summary

We have 12 questions on this page.

1. How many questions did you answer? ( / 12)
2. What percentage of questions did you answer? (  %)
3. List question numbers that you cannot answer:
4. (Optional) Explain why you could not answer these questions.
