# Q4. PDO Troubleshooting and Security Essentials (Optional)

---

## Part 1: PDO Troubleshooting

---

## Question 4.1

If running PHP code with `new PDO("mysql:...")` throws a PDOException with the message **"could not find driver"**, what terminal commands verify whether the PDO MySQL driver is loaded in your PHP environment on Linux/WSL2 and macOS?

**Answer:**

---

## Question 4.2

On Ubuntu or WSL2, which terminal command installs the missing PDO MySQL extension, and what action must you take immediately after installation before testing the PHP page again?

**Answer:**

---

## Question 4.3

When troubleshooting a connection failure in `index_pdo.php` or `crud_pdo.php`, what three items should you systematically verify in order?

**Answer:**

---

## Part 2: Security Essentials

---

## Question 4.4

How do prepared statements separate SQL structure from parameter values, why does this prevent SQL injection, and how does this mechanism safely handle data containing quotes (such as `O'Connor`)?

**Answer:**

---

## Question 4.5

Why do prepared statements NOT replace the need for `htmlspecialchars()` when rendering database values in an HTML browser page? If `$student['name']` contains `<b>Alice</b>`, contrast what the browser displays with direct output versus safe output:

**Answer:**

---

## Part 3: Deep Dive & MySQLi Reference

---

## Question 4.6

What is the default value of `PDO::ATTR_EMULATE_PREPARES` in the PDO MySQL driver, and what does it do?

**Answer:**

---

## Question 4.7

When is setting `PDO::ATTR_EMULATE_PREPARES => false` (native prepares) useful, and what is its performance advantage in bulk operations?

**Answer:**

---

## Question 4.8

What are two primary differences between PDO and MySQLi regarding database portability and parameter binding syntax?

**Answer:**

---

## Question 4.9

Complete the following MySQLi reference code by filling in the placeholder symbol and the method used to bind variables:

```php
$stmt = $conn->prepare("SELECT * FROM students WHERE id = ??1");
$stmt->??2("i", $aliceId);
$stmt->execute();
```

**Answer:**

---

## Summary

We have 9 questions on this page.

1. How many questions did you answer? ( / 9)
2. What percentage of questions did you answer? (  %)
3. List question numbers that you cannot answer:
4. (Optional) Explain why you could not answer these questions.
