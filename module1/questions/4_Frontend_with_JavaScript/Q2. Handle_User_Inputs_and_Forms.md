# Q2. Handle User Inputs and Forms

---

## Question 2.1

This is the HTML form from Step 1. It sends its fields through the Front Controller to `POST /index.php/submit`. Fill in ??#

```html
<form action="??1" method="??2">
  Name: <input type="text" name="name"><br>
  Email: <input type="email" name="email"><br>
  <input type="submit" value="Submit">
</form>
```

**Answer:**

---

## Question 2.2

If an `<input>` does not have a `name` attribute, its value is still included in the `FormData` sent to the server. (T/F)

**Answer:**

---

## Question 2.3

This is JavaScript to handle form submission with event prevention (Step 2).

We need to add an event listener for the `submit` event to call an anonymous function.

Fill in ??#.

```javascript
document.querySelector('form').??1('??2', async function (event) {
    event.preventDefault();
});
```

**Answer:**

---

## Question 2.4

This is the JavaScript code from Step 3 that builds and sends the POST request using the FormData API. Fill in ??#

```javascript
const form = ??1;
const formData = new ??2(form);
const response = await fetch(form.??3, {
  method: 'POST',
  body: formData
});
```

**Answer:**

---

## Question 2.5

In Step 3, what does `new FormData(form)` collect from the `<form>` element?

**Answer:**

---

## Question 2.6

This is the PHP code from Step 4 that reads the submitted fields and returns a JSON response without an intermediate `$info` variable. Fill in ??#.

```php
$name = $??1['name'] ?? '';
$email = $??1['email'] ?? '';

??2([
    'name' => $name,
    'email' => $email,
], 'Response from POST request');
```

**Answer:**

---

## Question 2.7

This is the Front Controller code from Step 4.1 that allows only POST for the `submit` route. Fill in ??#.

```php
case 'submit':
    if ($method !== '??1') {
        sendError('Method not allowed for this endpoint', ??2);
    }
```

**Answer:**

---

## Question 2.8

This is the JavaScript code from Step 5 that displays the response or an error. Fill in ??#

```javascript
try {
  if (!response.??1) {
    throw new ??2(`HTTP error! status: ${response.status}`);
  }

  const data = await response.??3();
  document.getElementById('result').textContent =
    JSON.stringify(data, null, 2);
} catch (error) {
  document.getElementById('result').textContent =
    '??4: ' + error.message;
}
```

**Answer:**

---

## Question 2.9

Put the five steps of the form workflow in the correct order.

(a) PHP reads the fields and returns JSON
(b) JavaScript displays the result or an error
(c) The user enters values and submits the form
(d) JavaScript sends the fields in a POST request
(e) JavaScript intercepts the `submit` event

**Answer:**

---

## Summary

We have 9 questions in this page.

1. How many questions did you answer? ( / 9)
2. What percentage of questions did you answer? (  %)
3. List question numbers that you cannot answer:
4. (Optional) Explain why you could not answer these questions.
