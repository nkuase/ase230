# Q3. Bearer Token Authentication

---

## Question 3.1

Why do modern REST APIs prefer token-based authentication (such as Bearer tokens) over browser cookies and sessions?

**Answer:**

---

## Question 3.2

```txt
User enters: username="john", password="Secret123"
System checks: Credentials valid?
If yes: "Here's your token!" (Bearer token issued: john-demo-token)
If no: "Invalid credentials" (401 Unauthorized, no token)
```

In this scenario, a Bearer token acts like a temporary digital wristband or ticket: whoever holds the token is granted access to protected API endpoints. (T/F)

**Answer:**

---

## Question 3.3

Examine this HTTP request header:

```http
GET /students/5
Authorization: Bearer john-demo-token
```

Identify the three components of this authentication header:
1. Header name: ??1
2. Authentication scheme: ??2
3. Token credential: ??3

**Answer:**

---

## Question 3.4

These are the 4 steps in the Bearer Token authentication lifecycle. Fill in ??#.

1. **??1 once** with credentials → Server verifies and returns a token
2. **Store the token** safely on the client (in memory, localStorage, etc.)
3. **Send the token** with every API request in the `Authorization` header
4. **Server ??2** whether the token is valid before returning protected data

**Answer:**

---

## Question 3.5

Fill in ??# to complete the comparison between Cookies, Sessions, and Bearer Tokens.

| Method | How it works | Best for |
|---|---|---|
| **??1** | Browser automatically attaches data with requests | Traditional websites, non-sensitive preferences |
| **??2** | Server stores state mapped to session ID cookie | Server-rendered web applications |
| **??3** | Client manually attaches token in HTTP header | REST APIs, mobile apps, microservices |

**Answer:**

---

## Question 3.6

In `bearer_auth.php`, what is the name of the function that inspects the request headers and extracts the token string from the format `Bearer <token>`?

**Answer:**

---

## Question 3.7

In the Bearer token implementation from the lecture, examine `isValidToken($token)`:

```php
function isValidToken($token) {
    $validTokens = [
        'john-demo-token' => 'john',
        'admin-demo-token' => 'admin'
    ];
    return $validTokens[$token] ?? false;
}
```

1. How does this demo function check whether a token is valid, and how does this differ from a real application's login flow?
2. When `isValidToken($token)` is called, what does it return for a valid token vs an invalid/unrecognized token?

**Answer:**

---

## Question 3.8

This is the `requireAuth()` function from `bearer_auth.php`. Fill in ??#.

```php
function requireAuth() {
    $token = ??1();
    if (!$token) {
        sendJsonError(401, 'Bearer token required');
    }
    $user = ??2($token);
    if (!$user) {
        sendJsonError(401, 'Invalid or expired token');
    }
    return $user;
}
```

**Answer:**

---

## Question 3.9

Examine the role of `requireAuth()` and `sendJsonError()` on protected endpoints:

1. Where should `requireAuth()` be called in a protected API endpoint file (such as `protected_api.php`)?
2. Why must `sendJsonError()` execute `exit` after outputting the JSON error?

**Answer:**

---

## Question 3.10

You have a protected API endpoint running at `http://localhost:8080/protected_api.php`. Write the missing header in the cURL command to send the valid token `john-demo-token`.

```bash
curl -H "???" http://localhost:8080/protected_api.php
```

**Answer:**

---

## Question 3.11

This JavaScript code calls the protected API endpoint using `fetch()`. The token string is stored in variable `token`. Fill in ??1 to provide the proper `Authorization` header value.

```javascript
const token = 'john-demo-token';

fetch('http://localhost:8080/protected_api.php', {
    headers: {
        'Authorization': ??1
    }
})
.then(res => res.json())
.then(data => console.log(data));
```

**Answer:**

---

## Question 3.12

When verifying a protected Bearer token API endpoint, what are the three essential test cases that must always be tested, and what HTTP status code is expected for each?

**Answer:**

---

## Question 3.13

The course includes an automated test script (`test_curl.sh`) to verify the Bearer token API:

1. Why does the script parse and capture the token dynamically from the login response rather than using a hardcoded token?
2. Why does the script exit with a non-zero exit code (`exit 1`) if any test check fails?

**Answer:**

---

## Summary

We have 13 questions in this page.

1. How many questions did you answer? ( / 13)
2. What percentage of questions did you answer? (  %)
3. List question numbers that you cannot answer:
4. (Optional) Explain why you could not answer these questions.
