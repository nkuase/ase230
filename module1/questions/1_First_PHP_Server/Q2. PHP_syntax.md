# Q2. PHP_syntax

---

## Question 2.1

Declare a variable named `$studentCount` with the value 25.

**Answer:**

---

## Question 2.2

The following code reassigns `$pi` with a different type. Why can this cause a hard-to-find bug?

```php
$pi = 3.14159;  // float
$pi = "3.14159"; // string
```

**Answer:**

---

## Question 2.3

What symbol must every PHP variable name start with?

**Answer:**

---

## Question 2.4

Declare a constant named `COUNT` with the value 25. (Do not use enum or legacy alternative)

**Answer:**

---

## Question 2.5

```php
enum Status: string {
  case Draft = 'draft';
  case Published = 'published';
}
```

We have Status enum; I need to store the Draft value in the Status enum to a variable $postStatus. Write the code.

**Answer:**

---

## Question 2.6

What is the difference between single quotes and double quotes in PHP strings?

**Answer:**

---

## Question 2.7

Create an indexed array ($colors) with three colors: 'red', 'blue', 'green'.

**Answer:**

---

## Question 2.8

Create an associative array ($student) for a student with id=123 and name='John Doe'.

**Answer:**

---

## Question 2.9

Rewrite this array as a DTO (Data Transfer Object) class so that `id` (int) and `name` (string) are required and type-checked, then create the object.

```php
$user = ['id' => 7, 'name' => 'Ada'];
```

**Answer:**

---

## Question 2.10

Between an array and a DTO, which one catches invalid data (e.g., passing a string where an int is expected) earlier, and why?

**Answer:**

---

## Question 2.11

Write an if-elseif-else statement to assign grades based on scores:
if $score is more than 90, $grade is A, if more than 80, grade is B, and C, for the rest of the grade.

**Answer:**

---

## Question 2.12

Rewrite this if-else statement using the ternary operator:

```php
if ($age >= 18) {
    $status = 'adult';
} else {
    $status = 'minor';
}
```

**Answer:**

---

## Question 2.13

Write code when $_GET['u'] is null, $user value becomes 'guest' using null coalescing.

**Answer:**

---

## Question 2.14

Why should you check a nullable value before using it, instead of just using it directly?

**Answer:**

---

## Question 2.15

This is PHP code to assign $mime based on the $ext. What is the keyword in ???

```php
$mime = match ($ext) {
  'png'  => 'image/png',
  'jpg', 'jpeg' => 'image/jpeg',
  'gif'  => 'image/gif',
  ??? => 'application/octet-stream',
};
```

**Answer:**

---

## Question 2.16

Write a function called `multiply` that takes two integers and returns their product:

**Answer:**

---

## Question 2.17

This is a PHP function that uses pass by value. Rewrite this function to use pass by reference.

```php
function incVal(int $x): void {
  $x++;
}
```

**Answer:**

---

## Question 2.18

```php
function decVal(int &$x): void {
  $x--;
}
$x = 20;
decVal($x);
echo $x;
```

What value is going to be printed?

**Answer:**

---

## Question 2.19

Rewrite this named function as a closure (anonymous function) assigned to a variable `$triple`.

```php
function triple(int $n): int {
  return $n * 3;
}
```

**Answer:**

---

## Question 2.20

Rewrite the closure from Question 2.19 as an arrow function.

**Answer:**

---

## Summary

We have 20 questions in this page.

1. How many questions did you answer? ( / 20)
2. What percentage of questions did you answer? (  %)
3. List question numbers that you cannot answer:
4. (Optional) Explain why you could not answer these questions.
