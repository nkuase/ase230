# Q2. Request and Response in PHP

---

## Question 2.1

Which real PHP file does this lesson follow? What command starts its server,
and what is the first curl request you should send?

**Answer:**

---

## Question 2.2

Which superglobal provides `REQUEST_METHOD`, `REQUEST_URI`, and
`HTTP_USER_AGENT`?

**Answer:**

---

## Question 2.3

For `/request_info.php?name=Alice`, what are the values of the method and
`$_GET`?

**Answer:**

---

## Question 2.4

Complete the actual path expression from `request_info.php`, then explain
why it is needed.

```php
'path' => ??1($_SERVER['REQUEST_URI'], ??2),
```

For `/request_info.php?name=Alice`, `$_SERVER['REQUEST_URI']` already
contains `/request_info.php?name=Alice`. What does this expression return,
and why is it still needed instead of using `REQUEST_URI` directly?

**Answer:**

---

## Question 2.5

Why does the code use this expression instead of `$_SERVER['HTTP_USER_AGENT']`
alone?

```php
$_SERVER['HTTP_USER_AGENT'] ?? null
```

**Answer:**

---

## Question 2.6

Why should a GET request not save or change data on the server?

**Answer:**

---

## Question 2.7

Choose HTML or JSON for each response and explain the choice:

1. A page intended to be read directly by a person in a browser.
2. Structured API data intended for another program.

**Answer:**

---

## Question 2.8

What value does the server receive for `name` in each of these query
strings?

- `?name=Alice%20Smith`
- `?name=Alice+Smith` (form-style query string)

**Answer:**

---

## Question 2.9

Which response header tells the client that the body contains JSON, and why
must `request_info.php` send it before the response body?

**Answer:**

---

## Question 2.10

What does `JSON_PRETTY_PRINT` change in the response, and what does
`JSON_UNESCAPED_SLASHES` change for a path such as `/request_info.php`?

**Answer:**

---

## Question 2.11

Predict the full JSON response (`method`, `path`, `query.name`, and
`query.year`) for this request:

```bash
curl "http://localhost:8000/request_info.php?name=Alice%20Smith&year=2"
```

**Answer:**

---

## Summary

We have 11 questions on this page.

1. How many questions did you answer? ( / 11)
2. What percentage of questions did you answer? (  %)
3. List question numbers that you cannot answer:
4. (Optional) Explain why you could not answer these questions.
