# Q3. POST and PUT JSON Requests

---

## Question 3.1

Where do POST and PUT normally carry JSON data?

**Answer:**

---

## Question 3.2

Write the command from the lesson that sends `{"name":"Alice"}` to
`methods.php` with POST.

**Answer:**

---

## Question 3.3

A request body sent with POST or PUT is not shown in the URL. Does this mean
it is automatically secret and safe? What should you use to actually protect
it?

**Answer:**

---

## Question 3.4

Which two request methods does `methods.php` allow, and what status code and
header are returned for any other method?

**Answer:**

---

## Question 3.5

Why does JSON input not come from `$_POST`?

**Answer:**

---

## Question 3.6

Complete the code that reads the content type and decides whether the body
is JSON:

```php
$contentType = $_SERVER['??1'] ?? '';
$isJson = ??2($contentType, 'application/json');
```

Does `Content-Type` describe the request method, or the format of the
request body?

**Answer:**

---

## Question 3.7

Complete the JSON input code:

```php
$rawBody = ??1('php://input');
$data = ??2($rawBody, true);
```

**Answer:**

---

## Question 3.8

Why is the second argument to `json_decode()` set to `true`?

**Answer:**

---

## Question 3.9

`methods.php` reads the raw body once and then chooses one of three
branches. Complete the logic:

```php
if ($isJson) {
    $data = ??1;
} elseif ($method === 'POST') {
    $data = ??2;
} else { // $method is 'PUT'
    ??3;
}
```

**Answer:**

---

## Question 3.10

A client sends this invalid JSON:

```bash
curl -X POST http://localhost:8000/methods.php \
  -H "Content-Type: application/json" \
  -d '{bad}'
```

Trace what happens: what does `json_decode()` return, what check catches
it, what response is sent, and why does the script call `exit` afterward?

**Answer:**

---

## Question 3.11

Which three values does the successful JSON response confirm?

**Answer:**

---

## Question 3.12

What does PUT commonly do to an existing resource?

**Answer:**

---

## Question 3.13

What does POST commonly do, and how is this different from GET?

**Answer:**

---

## Question 3.14

In the PUT example, `methods.php` reads `{"name":"Alice Updated"}` from
`php://input` and returns it in the `data` field of the response. Does this
example PUT request update a database?

**Answer:**

---

## Summary

We have 14 questions on this page.

1. How many questions did you answer? ( / 14)
2. What percentage of questions did you answer? (  %)
3. List question numbers that you cannot answer:
4. (Optional) Explain why you could not answer these questions.
